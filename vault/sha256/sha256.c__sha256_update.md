# sha256.c__sha256_update

Tags: #loop

```json
{
  "title": "SHA-256 Update Function",
  "summary": "The sha256_update function is used to update the internal state of a SHA-256 hash object with new data.",
  "details": "This function takes a pointer to a SHA-256 hash object, a pointer to the data to be hashed, and the size of the data. It iterates over the data, updating the hash object's internal state by writing each byte to the current position in the buffer. When the buffer is full (64 bytes), it calls the sha256_write_byte_block function to process the block.",
  "rationale": "The function is implemented this way to efficiently update the hash object's state in small chunks, allowing for incremental hashing of large data sets.",
  "performance": "The function has a time complexity of O(n), where n is the size of the data, making it suitable for large data sets. The use of a 64-byte buffer allows for efficient processing of blocks.",
  "hidden_insights": [
    "The function uses a 64-byte buffer to process blocks, which is a common size for cryptographic hash functions.",
    "The use of the bitwise AND operator (& 0x3F) to mask the current buffer position is a common technique to ensure that the position stays within the bounds of the buffer."
  ],
  "where_used": [
    "sha256.c",
    "sha256.h",
    "main.c"
  ],
  "tags": [
    "SHA-256",
    "hash function",
    "incremental hashing",
    "block processing"
  ],
  "markdown": "### SHA-256 Update Function\n\nThe `sha256_update` function is used to update the internal state of a SHA-256 hash object with new data.\n\n#### Purpose\n\nThis function takes a pointer to a SHA-256 hash object, a pointer to the data to be hashed, and the size of the data. It iterates over the data, updating the hash object's internal state by writing each byte to the current position in the buffer. When the buffer is full (64 bytes), it calls the `sha256_write_byte_block` function to process the block.\n\n#### Implementation\n\nThe function uses a 64-byte buffer to process blocks, which is a common size for cryptographic hash functions. The use of the bitwise AND operator (`& 0x3F`) to mask the current buffer position is a common technique to ensure that the position stays within the bounds of the buffer.\n\n#### Performance\n\nThe function has a time complexity of O(n), where n is the size of the data, making it suitable for large data sets.\n\n#### Usage\n\nThis function is likely used in the `sha256.c` file, as well as in other files that require incremental hashing, such as `main.c`."
}
